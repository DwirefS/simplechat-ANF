# Azure Cosmos DB
# Use connection string OR endpoint/key OR managed identity
# e.g., https://mycosmosdb.documents.azure.com:443/
AZURE_COSMOS_ENDPOINT="<your-cosmosdb-account-uri>"
# Options: "key", "connection_string", "managed_identity"
AZURE_COSMOS_AUTHENTICATION_TYPE="key"
# if using key, then populate the following:
AZURE_COSMOS_KEY="<your-cosmosdb-primary-key>"


# Azure AD Authentication (Required)
CLIENT_ID="<your-app-registration-client-id>"
TENANT_ID="<your-azure-ad-tenant-id>"
# SECRET_KEY should be a long, random, secret string (e.g., 32+ chars) used for Flask session signing. Generate one securely.
SECRET_KEY="Generate-A-Strong-Random-Secret-Key-Here!"
# AZURE_ENVIRONMENT: Set based on your cloud environment
# Options: "public", "usgovernment", "custom"
AZURE_ENVIRONMENT="public"


# =============================================================================
# Storage Backend Configuration
# =============================================================================
# Toggle between Azure Blob Storage and Azure NetApp Files Object REST API
# Options: "blob" (default) or "anf"
STORAGE_BACKEND="blob"

# =============================================================================
# Azure NetApp Files Configuration (used when STORAGE_BACKEND="anf")
# =============================================================================
# IMPORTANT: Object REST API requires manual setup via Azure Portal:
# 1. Submit waitlist request for preview access (~1 week)
# 2. Generate PEM-formatted SSL certificate in Azure Portal
# 3. Create bucket from Volume > Buckets in Azure Portal
# 4. Generate access credentials after bucket creation
# 5. Install certificate on client machines
#
# See: https://learn.microsoft.com/en-us/azure/azure-netapp-files/object-rest-api-access-configure

# ANF Object REST API endpoint (from Azure Portal after bucket creation)
ANF_OBJECT_API_ENDPOINT=""

# ANF credentials (generated in Azure Portal: Volume > Buckets > Generate access keys)
ANF_ACCESS_KEY=""
ANF_SECRET_KEY=""

# SSL verification (set to "false" if using self-signed certificates for testing)
ANF_VERIFY_SSL="true"

# ANF service level (configured during Bicep deployment)
# Options: "Standard", "Premium", "Ultra", "Flexible"
ANF_SERVICE_LEVEL="Premium"

# ANF bucket names (created via Azure Portal, map to Blob container names)
ANF_USER_DOCUMENTS_BUCKET="user-documents"
ANF_GROUP_DOCUMENTS_BUCKET="group-documents"
ANF_PUBLIC_DOCUMENTS_BUCKET="public-documents"